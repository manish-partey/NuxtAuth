// Stub module for Nuxt virtual module `#app-manifest` used only by Vite import analysis.
// At runtime, Nuxt fetches the build manifest and does not import this stub.
// Keeping it minimal prevents dev errors without affecting production builds.
export default {} as any;